export interface QuestModel{
    id: number; 
    missionId?: number; 
    name: string; 
    eventName?: string; 
    questValue: number;
    jsonConfig?: string; 
    totalRecord?: number; 
    missionName?: string;
}